name: Build Samsung Arabic Universal One UI 7 One Click
on:
  push:
    branches: [ samsung-arabic-core-v1-build ]
    paths:
      - 'samsung-arabic-build/TRIGGER_BUILD'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - name: Install Android SDK 35
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platforms;android-35" "build-tools;35.0.0" "platform-tools"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Build one-click manager APK
        run: gradle -p samsung-arabic-build :app:assembleDebug --stacktrace
      - name: Validate manager shell
        shell: bash
        run: |
          set -euo pipefail
          APK=samsung-arabic-build/app/build/outputs/apk/debug/app-debug.apk
          test -s "$APK"
          unzip -t "$APK"
          "$ANDROID_HOME/build-tools/35.0.0/aapt2" dump badging "$APK" | tee /tmp/badging.txt
          grep -q "package: name='com.basair.samsungarabic.oneclick'" /tmp/badging.txt
          grep -q "targetSdkVersion:'35'" /tmp/badging.txt
          "$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --verbose --print-certs "$APK" | tee /tmp/signature.txt
          grep -q 'Verified' /tmp/signature.txt
          mkdir -p artifact
          cp "$APK" artifact/SamsungArabicUniversal-OneUI7-v1.1-OneClick-shell.apk
          sha256sum artifact/SamsungArabicUniversal-OneUI7-v1.1-OneClick-shell.apk | tee artifact/SamsungArabicUniversal-OneUI7-v1.1-OneClick-shell.sha256
          cp /tmp/badging.txt artifact/SamsungArabicUniversal-OneUI7-v1.1-OneClick-shell-badging.txt
          cp /tmp/signature.txt artifact/SamsungArabicUniversal-OneUI7-v1.1-OneClick-shell-signature.txt
      - uses: actions/upload-artifact@v4
        with:
          name: SamsungArabicUniversal-OneUI7-v1.1-OneClick-shell
          path: artifact/*
          if-no-files-found: error
          retention-days: 14
