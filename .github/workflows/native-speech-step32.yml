name: native-speech-step32
on:
  push:
    branches: [ native-speech-build-step32-temp ]
    paths:
      - 'native-build-step32/**'
      - '.github/workflows/native-speech-step32.yml'
  workflow_dispatch:

jobs:
  build-dex:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Prepare Android SDK
        run: |
          yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" --licenses >/dev/null || true
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" "platforms;android-35" "build-tools;35.0.0"
      - name: Compile native activity
        run: |
          mkdir -p native-build-step32/out native-build-step32/dex
          javac -source 8 -target 8 \
            -classpath "$ANDROID_HOME/platforms/android-35/android.jar" \
            -d native-build-step32/out \
            native-build-step32/app/basair/quran/MainActivity.java
          "$ANDROID_HOME/build-tools/35.0.0/d8" \
            --min-api 23 \
            --output native-build-step32/dex \
            native-build-step32/out/app/basair/quran/MainActivity.class \
            native-build-step32/out/app/basair/quran/MainActivity\$NativeSpeechBridge.class \
            native-build-step32/out/app/basair/quran/MainActivity\$MicChromeClient.class \
            native-build-step32/out/app/basair/quran/MainActivity\$1.class \
            native-build-step32/out/app/basair/quran/MainActivity\$2.class \
            native-build-step32/out/app/basair/quran/MainActivity\$3.class \
            native-build-step32/out/app/basair/quran/MainActivity\$4.class \
            native-build-step32/out/app/basair/quran/MainActivity\$5.class \
            native-build-step32/out/app/basair/quran/MainActivity\$MicChromeClient\$1.class
          ls -lh native-build-step32/dex/classes.dex
      - name: Upload classes.dex
        uses: actions/upload-artifact@v4
        with:
          name: step32-native-classes-dex
          path: native-build-step32/dex/classes.dex
          if-no-files-found: error
